# Podstawowe zależności projektu Django
Django>=4.2,<5.0
djangorestframework>=3.14.0
django-cors-headers>=4.3.0

# Obsługa bazy danych PostgreSQL
psycopg2-binary>=2.9.9
dj-database-url>=2.1.0

# Obsługa zmiennych środowiskowych (.env / Docker)
python-dotenv>=1.0.0

# Serwer produkcyjny dla Dockera
gunicorn>=21.2.0

# Dodatkowe biblioteki używane w kodzie (walidatory, daty)
# (Django domyślnie obsługuje większość z nich, ale warto je mieć w środowisku)
pytz>=2023.3
Django>=4.2,<5.0
djangorestframework>=3.14.0
django-cors-headers>=4.3.0
psycopg2-binary>=2.9.9
dj-database-url>=2.1.0
python-dotenv>=1.0.0
gunicorn>=21.2.0